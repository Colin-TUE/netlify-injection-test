{
  "name": "website-2",
  "description": "website builder 2nd version",
  "version": "1.0.0",
  "author": "Colin-TUE",
  "license": "MIT",
  "scripts": {
    "build": "tsc && node build/test.js",
    "build:dev": "ts-node src/test.ts",
    "start": "serve public -p 8888",
    "clean": "rm -rf public build",
    "clean:netlify": "rm -rf .cache public build && cd .netlify && rm -r `ls | grep -v \"^state.json$\"`",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
  },
  "devDependencies": {
    "ts-node": "10.9.1",
    "serve": "14.2.1",
    "typescript": "5.1.6",
    "@types/node": "17.0.31"
  }
}
